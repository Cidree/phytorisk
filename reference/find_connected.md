# (Internal) Finds connected pixels

(Internal) Finds connected pixels

## Usage

``` r
find_connected(r, init_i, init_j)
```

## Arguments

- r:

  a SpatRaster

- init_i:

  ith row to start

- init_j:

  jth row to start

## Value

a list
